import path from 'path';

/**
 * * Directory where uploaded files are stored
 * * @constant {string} uploadDir - Path to the uploads directory
 */
const UPLOAD_DIRECTORY = path.join(__dirname, "../uploads");

export default UPLOAD_DIRECTORY;